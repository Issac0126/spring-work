<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    
<mapper namespace="com.spring.db.repository.IBoardMapper">

	<resultMap type="com.spring.db.model.BoardVO" id="boardMap">
		<id property="boardNo" column="board_no"/>
		<result property="regDate"  column="reg_date" />
	</resultMap>


    <!-- 게시글 등록 -->
	<insert id="insertArticle">
		INSERT INTO jdbc_board (writer, title, content) 
		VALUES (#{vo.writer}, #{vo.title}, #{vo.content})		
	</insert>
    
	<!-- 전체 게시글 조회 -->
	<select id="getArticles" resultMap="boardMap">
		SELECT * FROM jdbc_board
		ORDER BY board_no ASC
	</select>

	<!-- 게시글 상세 보기 -->
	<select id="getArticle" resultMap="boardMap">
		SELECT * FROM jdbc_board
		WHERE board_no = #{bNo}
	</select>

	<!-- 게시글 삭제 -->	
	<delete id="deleteArticle">
		DELETE FROM jdbc_board
		WHERE board_no = #{bNo}
	</delete>	
	
	<!-- 게시글 수정 -->
	<update id="updateArticle">
		UPDATE jdbc_board 
		SET  writer=#{vo.writer}, title=#{vo.title}, content=#{vo.content}
		WHERE board_no=#{vo.boardNo}
	</update>
	
	
	
	
	
	
</mapper>
	
	
	
	
	
	
	
	